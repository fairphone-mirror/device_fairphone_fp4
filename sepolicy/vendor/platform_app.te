# IrisUI app is a platform_app, allow calls to iris hwservice
hal_client_domain(platform_app, hal_display_iris)

# Allow NFC app to use service_manager
allow platform_app nfc_service:service_manager find;
